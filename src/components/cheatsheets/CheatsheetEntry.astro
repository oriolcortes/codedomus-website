---
const { primary, secondary } = Astro.props;
---

<div
  class="flex flex-col border-b border-gray-200 pb-1 last:border-0 sm:flex-row sm:items-center sm:justify-between"
  data-primary={primary}
  data-secondary={secondary ?? ""}
>
  <code class="font-mono text-sm break-all text-blue-800">{primary}</code>
  {secondary && <span class="text-sm text-gray-600 sm:ml-4">{secondary}</span>}
</div>
